{
  "id": "01-micrograd-ex002",
  "lectureId": "01-micrograd",
  "segmentId": "01-micrograd-seg02",
  "title": "Derivatives at Multiple Points",
  "difficulty": "easy",
  "order": 2,
  "topics": ["derivatives"],
  "description": "Implement `derivative_table(f, points, h=1e-5)` that returns a list of `(x, df/dx)` tuples for each x in `points`. Use the centered difference formula.",
  "starterCode": "def derivative_table(f, points, h=1e-5):\n    # Return a list of (x, derivative) tuples\n    pass\n",
  "solutionCode": "def derivative_table(f, points, h=1e-5):\n    return [(x, (f(x + h) - f(x - h)) / (2 * h)) for x in points]\n",
  "testCode": "try:\n    result = derivative_table(lambda x: x**2, [1, 2, 3])\n    assert len(result) == 3\n    print(\"PASS: returns correct number of tuples\")\nexcept:\n    print(\"FAIL: returns correct number of tuples\")\n\ntry:\n    result = derivative_table(lambda x: x**2, [1, 2, 3])\n    expected = [2.0, 4.0, 6.0]\n    for (x, dfdx), exp in zip(result, expected):\n        assert abs(dfdx - exp) < 1e-4\n    print(\"PASS: derivatives of x^2 at [1,2,3] are [2,4,6]\")\nexcept:\n    print(\"FAIL: derivatives of x^2 at [1,2,3] are [2,4,6]\")\n\ntry:\n    result = derivative_table(lambda x: x**2, [1, 2, 3])\n    assert result[0][0] == 1 and result[1][0] == 2 and result[2][0] == 3\n    print(\"PASS: x values are preserved in tuples\")\nexcept:\n    print(\"FAIL: x values are preserved in tuples\")\n",
  "hints": [
    "Use a list comprehension",
    "Apply the centered difference formula for each point"
  ]
}
